const authValidator = require('./authValidator');
const studentValidator = require('./studentValidator');
const teacherValidator = require('./teacherValidator');
const attendanceValidator = require('./attendanceValidator');
const feeValidator = require('./feeValidator');
const examValidator = require('./examValidator');
const communicationValidator = require('./communicationValidator');

module.exports = {
  authValidator,
  studentValidator,
  teacherValidator,
  attendanceValidator,
  feeValidator,
  examValidator,
  communicationValidator,
};
