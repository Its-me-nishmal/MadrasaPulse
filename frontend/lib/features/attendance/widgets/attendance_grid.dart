import 'package:flutter/material.dart';

class AttendanceGrid extends StatelessWidget {
  const AttendanceGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Attendance grid'));
  }
}
